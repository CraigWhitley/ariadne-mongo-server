type User {
  id: ID!
  email: String!
  firstName: String!
  lastName: String!
  accessToken: String
}

extend type Query {
    allUsers(skip: Int!, take: Int!): [User!]!
    findUserByEmail(email: String!):User
}
